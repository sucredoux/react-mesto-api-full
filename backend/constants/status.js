const BAD_REQUEST = 400;

const NOT_FOUND = 404;

const DUPLICATE_USER = 409;

const SERVER_ERROR = 500;

const OK = 200;

const CREATED = 201;

const AUTH = 401;

const FORBIDDEN = 403;

const MONGO_DUPLICATE = 11000;

module.exports = {
  BAD_REQUEST,
  NOT_FOUND,
  SERVER_ERROR,
  OK,
  CREATED,
  AUTH,
  DUPLICATE_USER,
  MONGO_DUPLICATE,
  FORBIDDEN,
};
